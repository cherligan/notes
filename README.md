# Заметки (Notes App)

Простое веб-приложение для создания, редактирования и удаления заметок. Заметки сохраняются в localStorage браузера.

🚀 Стек технологий

- React 18  
- Vite (сборка и hot-reload)  
- CSS (чистый, без фреймворков)  
- react-icons (иконки)  
- localStorage (хранение данных)

🛠 Установка и запуск

1. Клонирование репозитория
```bash
git clone https://github.com/cherligan/notes.git
cd notes

```
2. Установка зависимостей
```bash
npm install
```
3. Запуск в режиме разработки
```bash
npm run dev
```
4. Сборка продакшена
```bash
npm run build
```
Готовая сборка окажется в папке dist
📖 Функционал

- Добавление новой заметки
- Просмотр списка заметок в виде карточек
- Редактирование заметки (модальное окно)
- Удаление заметки
- Сохранение заметок между перезагрузками страницы (localStorage)

📁 Структура проекта

- src/components/ — переиспользуемые компоненты
- src/components/Header/ — шапка приложения
- src/components/Note/ — карточка заметки и список
- src/components/Button/ — стилизованные кнопки
- App.jsx — главный компонент
- index.css — глобальные стили
- main.jsx — точка входа
